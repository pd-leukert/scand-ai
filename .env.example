# Copy to .env (gitignored) and edit. Docker Compose reads .env from the folder you run it in.
# The exact Ollama model tag for extraction. 4B is the one we tested on a laptop; on a GPU
# use the largest tag that fits (see docs/extraction.md).
EXTRACTION_MODEL=qwen3.5:4b
