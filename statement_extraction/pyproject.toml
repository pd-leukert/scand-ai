[project]
name = "statement-extraction"
version = "0.1.0"
description = "Offline statement extraction service"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "openai>=3.16.2",
]
